{
  "name": "musync",
  "version": "1.0.0",
  "private": true,
  "description": "Musync - Real-Time Synchronized Music Lounge & Retro Turntable Player",
  "main": "server/server.js",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
